#include "myapp.h"
#include <stdio.h>
#include "job.h"
#include "common.h"

struct job_t jobs[MAXJOBS];

int main(int argc, char *argv[]) {
  initjobs(jobs);
  
  while (true) {
    
  }

  return 0;
}
